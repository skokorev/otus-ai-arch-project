databaseChangeLog:
  - changeSet:
      id: 1
      author: SKokorev
      changes:
        - createTable:
            tableName: sets
            columns:
              - column:
                  name: id
                  type: varchar(50)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false
        - createTable:
            tableName: groups
            columns:
              - column:
                  name: id
                  type: varchar(40)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: varchar(255)
                  constraints:
                    nullable: false
        - createTable:
            tableName: user_group_mapping
            columns:
              - column:
                  name: group_id
                  type: varchar(40)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: email
                  type: varchar(255)
                  constraints:
                    primaryKey: true
                    nullable: false
        - createTable:
            tableName: group_sets
            columns:
              - column:
                  name: group_id
                  type: varchar(40)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: set_id
                  type: varchar(50)
                  constraints:
                    primaryKey: true
                    nullable: false
